abstract rule AbstractRequireToNodeRule {
	
	source {
		[+] port:BlockDiagram.Require
	}
	
	target {
		[+] portNode:MocaTree.Node {
			.name := "REQUIRE";
		}
	}
	
	correspondence {
		[+] portToNode :RequireToNode {
			src->port
			trg->portNode 
		}
	}
}

